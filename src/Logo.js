import React from 'react';
import logo from './logo.svg'

function Logo() {
    return (
        <div className="logo">
            <img src={logo} alt="logotype" />
        </div>
    );
}

export default Logo;